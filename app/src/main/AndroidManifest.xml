<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dailymood.tracker.daily.diary">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.MoodActivity">

        </activity>
        <activity android:name=".activity.MoodListActivity">

        </activity>
        <activity android:name=".activity.AddReminderActivity" />
        <activity android:name=".activity.ReminderListActivity" />
        <activity android:name=".activity.AddActivity"></activity>
        <activity android:name=".activity.ActivityList"></activity>
        <activity
            android:name=".activity.HowAreYouActivty"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ChooseActivity">

        </activity>
        <activity android:name=".activity.ChooseMood">

        </activity>
    </application>

</manifest>